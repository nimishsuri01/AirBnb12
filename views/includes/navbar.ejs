<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-brands fa-airbnb"></i></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <form class="d-flex ms-md-4 my-3 my-md-0 nav-search" action="/listings" method="GET" role="search">
        <input class="form-control me-2" type="search" name="search" list="india-location-options" placeholder="Search Delhi, Goa, Mumbai..." aria-label="Search places" value="<%= typeof search !== 'undefined' && search ? search : '' %>"/>
        <datalist id="india-location-options">
          <% for (let location of indiaLocations) { %>
            <option value="<%= location %>"></option>
          <% } %>
        </datalist>
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
      <div class="navbar-nav ms-auto">
        <% if(currUser) {%>
          <a class="nav-link" href="/users/me">My hotels</a>
          <a class="nav-link" href="/bookings">My trips</a>
          <a class="nav-link" href="/wishlist">Wishlist</a>
        <%} %>
        <a class="nav-link" href="/listings/new">Add hotel</a>
        <% if(!currUser) { %>
        <a class="nav-link" href="/signup"><b>Sign up</b></a>
        <a class="nav-link" href="/login"><b>Log in</b></a>
       <% } %>
        <% if(currUser) {%>
          <a class="nav-link" href="/logout">Log out</a>
        <%} %>
      </div>
    </div>
  </div>
</nav>
